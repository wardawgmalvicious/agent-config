Negative control. Matches no **skill** `paths:` glob in the payload.

If reading this file pulls a conditional skill into context, the
observation method is wrong — fix that before trusting any other row in
`expected_activations.md`.

It does match one **rule**, `coding-markdown`, whose glob is `**/*.md`.
That is expected. This file controls for skills, not for everything.
