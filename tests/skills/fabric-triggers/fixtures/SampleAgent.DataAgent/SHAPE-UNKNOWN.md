Placeholder. No `.DataAgent` export was available when this fixture set was
built, so the folder's real contents are unknown — see README, "Fixtures
built on an unverified shape".

`fabric-data-agent` globs `**/*.DataAgent/**`, so this file is enough to
prove the glob fires on the folder. Replace it with a real export when one
is available.
