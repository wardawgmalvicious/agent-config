model Model
	culture: en-US
	defaultPowerBIDataSourceVersion: powerBI_V3

table Sales

	column Region
		dataType: string
		sourceColumn: Region

	measure 'Total Sales' = SUM(Sales[Amount])
		formatString: \$#,0
